/**
 * Dimensiones de los banners de navegación — módulo plano sin "use client".
 * Medidas tomadas de los slots reales de los bloques:
 *   · Principal  766×272 — Homepage `data-slot="principal-banner"`
 *   · Secundario 766×100 — Homepage/Zona `data-slot="secondary-banner"` y help-banner
 *   · Sidebar    200×264 — Sidebar `SidebarBanner` (BANNER_W × BANNER_H)
 */

export const BANNER_PRINCIPAL_WIDTH = 766;
export const BANNER_PRINCIPAL_HEIGHT = 272;

export const BANNER_SECUNDARIO_WIDTH = 766;
export const BANNER_SECUNDARIO_HEIGHT = 100;

export const BANNER_SIDEBAR_WIDTH = 200;
export const BANNER_SIDEBAR_HEIGHT = 264;
